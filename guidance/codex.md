### Codex adapter

Invoke the relevant canonical `factory-*` skill directly. Command workflows and specialist workflows are exposed as skills, including `factory-standup`, `factory-entry`, `factory-feature-architect`, and `factory-code-reviewer`.

Use Codex capabilities by purpose—planning, user interaction, filesystem access, shell execution, and connected Linear operations—rather than assuming Claude command or tool identifiers.
