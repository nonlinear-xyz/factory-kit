---
description: Measure the factory-kit's token footprint — baseline vs on-demand, heaviest assets, trim candidates
argument-hint: (no arguments)
---

Load and follow the canonical `factory-kit-audit` skill. This adapter adds no behavior; the canonical skill owns the read-only audit and output shape.

