---
description: Close out a Linear issue — move to Done, add a closing comment, and clean up the local branch/worktree
argument-hint: <issue-id> (optional — auto-detected from branch if omitted)
---

Load and follow the canonical `factory-close` skill. Pass `$ARGUMENTS` as its invocation input. That skill owns all confirmation gates and side-effect ordering; do not add side effects in this adapter.

