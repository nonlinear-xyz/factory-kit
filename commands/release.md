---
description: Cut a release with synchronized versions, notes, commit, tag, push, GitHub Release, and npm publication gates
argument-hint: patch | minor | major (optional — asks if omitted)
---

Load and follow the canonical `factory-release` skill. Pass `$ARGUMENTS` as its invocation input. Preserve every approval gate; this adapter must not perform release side effects itself.

