---
description: Move the current branch's Linear issue to In Review
argument-hint: <issue-id> (optional — auto-detected from branch if omitted)
---

Load and follow the canonical `factory-submit` skill. Pass `$ARGUMENTS` as its invocation input. Preserve the skill's confirmation gates and do not add PR or git side effects.

