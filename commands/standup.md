---
description: Dev standup — open Linear tickets grouped by in-flight, top priority, and backlog
---

Load and follow the canonical `factory-standup` skill. This adapter adds no host-specific Linear tool names; use the capabilities selected by the skill.

