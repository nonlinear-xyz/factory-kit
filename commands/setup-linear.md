---
description: Configure .factory-kit/linear.json for the portable Linear workflows
---

Load and follow the canonical `factory-setup-linear` skill. It owns discovery, legacy-config migration, validation, and the write to `.factory-kit/linear.json`.

